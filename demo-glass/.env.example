# Optional: only if you deploy demo-glass on a *different* host than the Python API.
# Unified repo-root Vercel uses same-origin POST /api/chat (leave unset).
# VITE_CHAT_API_BASE=https://mini-claude-code.vercel.app
